def lambda_handler(event, context):
    a = event.get("a", 5)
    b = event.get("b", 3)

    result = a + b   # computation

    print(f"Adding {a} + {b} = {result}")

    return {
        "result": result
    }


INPUT:

{
  "a": 10,
  "b": 20
}


TO TAKE INPUT:

import json

def lambda_handler(event, context):
    a = event["a"]
    b = event["b"]

    result = a + b

    return {
        "result": result
    }
