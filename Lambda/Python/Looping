def lambda_handler(event, context):
    n = event.get("n", 5)

    total = 0
    for i in range(1, n+1):
        total += i

    print(f"Sum of first {n} numbers = {total}")

    return {
        "sum": total
    }


INPUT:

{
  "n": 10
}
